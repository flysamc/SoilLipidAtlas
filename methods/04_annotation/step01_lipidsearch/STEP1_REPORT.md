# Annotation Step 1 (LipidSearch) — release `ncbi-phylum-2026-08-04-v1`

Supplementary Method 3, Step 1: LipidSearch database matching.
Generated by `paper2_repro/scripts/step1_lipidsearch_release.py`.
Checksums, parameters and source paths: `STEP1_MANIFEST.json`.

Matching parameters, identical to the recovered producer and to the submitted
Supplementary Method 3: 5 ppm m/z, 0.3 min RT, RT window 1.5–25 min.
Tiers: Gold = Grade A/B, IDScore ≥ 0.20, ≥ 2 samples; Silver = IDScore ≥ 0.10 with
Grade A/B or ≥ 2 samples; Bronze = IDScore ≥ 0.01.

## Provenance: two different classes, do not conflate

| Mode | Provenance | Basis |
|---|---|---|
| POSITIVE | **Exact reuse.** Nothing recomputed. | `consensus_lipidsearch_direct.csv` from the recovered `step6b_direct_lipidsearch_mapping.py`, covering all 273,248 POS consensus features |
| NEGATIVE | **New producer.** This mapping did not previously exist anywhere in the package. | step6b semantics ported to NEG: 177 `*NEG.raw.txt` exports → 74,106 rows → 18,179 clusters (17,214 after reject) → matched against 122,571 NEG consensus features |

The NEG result is a **new computation** and must be declared as such in any
manuscript text. It is not a recovered historical result.

## Coverage

| Feature set | Features | Annotated | % | Grade A/B | % | Gold | Silver | Bronze |
|---|---|---|---|---|---|---|---|---|
| POS strict atlas | 11,371 | 3,619 | 31.83 | 1,304 | 11.47 | 979 | 1,334 | 1,306 |
| POS newly selected | 6,636 | 2,275 | 34.28 | 880 | 13.26 | 636 | 752 | 887 |
| NEG strict atlas | 5,697 | 961 | 16.87 | 512 | 8.99 | 394 | 316 | 251 |
| *POS strict atlas via superseded step6 mapping* | 11,371 | 2,349 | 20.66 | 592 | 5.21 | — | — | — |

These are consistent with the submitted claim that LipidSearch alone yields
**7.4% annotation** before the remaining ten steps; the Grade A/B rates above are
the Gold-eligible floor, not the final tier rate.

## NEG mapping validation

All matches respect the declared tolerances (max 4.998 ppm, max 0.300 min;
medians 0.926 ppm and 0.113 min). Adduct distribution is genuinely negative-mode
with no positive-mode leakage: `M-H` 5,481, `M+HCOO` 2,844, `M-CH3` 312, `M-2H` 12.
Class distribution is NEG-appropriate (PC via formate adduct, FA, Cer, PG, PE, PS,
CL, DGDG, MGDG, PI).

Gain over the previous state: the cached NEG unified annotation table listed
LipidSearch as an annotation source for **74** features. This mapping annotates
**961** strict NEG features.

## Consequence for the archaeal phyla

LipidSearch barely covers archaea, in either polarity:

| Phylum | POS features | POS annotated | POS Grade A/B | NEG features | NEG annotated |
|---|---|---|---|---|---|
| Methanobacteriota | 1,017 | 58 (5.70%) | 14 (1.38%) | 18 | 1 (5.56%) |
| Thermoproteota | 316 | 22 (6.96%) | 1 (0.32%) | 3 | 0 (0.00%) |

This is expected: the LipidSearch database does not cover archaeal ether lipids
(GDGT, archaeol, hydroxy-archaeol, caldarchaeol). It is the quantitative reason
Steps 7 and 8 (custom archaeal database and ArchLips) exist, and it means the
archaeal annotation rate in the revised manuscript will be set almost entirely by
Step 8, not by Step 1.

Note also **Heterolobosea**, the phylum at the centre of the peer-review *Naegleria*
correction: 31 POS features at 12.90% annotated with **0** at Grade A/B, and 561 NEG
features at **0.53%** annotated with 0 at Grade A/B. The phylum whose taxonomy the
correction changes is among the least annotated in the atlas.

## Mapping choice (declared)

Two POS LipidSearch mappings exist in the recovered material and they disagree
materially:

- `consensus_lipidsearch_direct.csv` (step6b, parses raw exports) → **1,304** Grade A/B on the strict atlas
- `consensus_lipidsearch_annotations.csv` (step6, routes through a stale GNPS classical-network JSON) → **592**

Supplementary Method 3 Step 1 does not name which mapping produced the
submitted numbers. This release uses the **direct** mapping, because step6b
was written explicitly to replace step6 and parses the raw vendor exports
without the stale network intermediate. The choice and its rationale are
recorded in `STEP1_MANIFEST.json`; the revised Methods states the mapping
used.

## Files

| File | Contents |
|---|---|
| `pos_strict_atlas_lipidsearch.csv` | 11,371 strict POS features + LipidSearch fields |
| `pos_newly_selected_lipidsearch.csv` | 6,636 newly selected POS features + LipidSearch fields |
| `neg_consensus_lipidsearch_direct.csv` | **New**: all 122,571 NEG consensus features, 8,649 matched |
| `neg_strict_atlas_lipidsearch.csv` | 5,697 strict NEG features + LipidSearch fields |
| `*_by_phylum.csv` | Per-phylum coverage for each set |
| `step1_coverage_summary.csv` | The coverage table above |
| `STEP1_MANIFEST.json` | Parameters, input/output sha256, provenance labels, open decision |
