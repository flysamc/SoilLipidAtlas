# Supplementary Figure 6 — render notes (strict-16)

| Item | Value |
|---|---|
| Panel a: NEG↔POS Bray-Curtis (n ≥ 5) | **0.213** (supersedes the published 0.103, computed on retired labels) |
| Panel d: matched features | 1,294 (35.2 % confirmed) |
| NEG SIMPER atlas | recomputed on the strict units; legacy-label gate passed at 1.1e-13 against the delivered atlas |

Renderer provenance: edits are lookup-level only (header provenance and one
comment string); the plotted value itself is read from the data.
